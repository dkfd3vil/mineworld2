Issues:

The mouse is working too fast if in fullscreen mode and sometimes in windowed mode too,

If you connect a client with server the server writer 2 times connected that not how it should behave,